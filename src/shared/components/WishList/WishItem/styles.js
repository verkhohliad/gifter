import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wishItem: {
  },
  title: {
    fontSize: 18,
    textAlign: 'center',
    marginTop: 10,
  },
});

export default styles;
