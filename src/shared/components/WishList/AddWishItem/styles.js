import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  addWishItem: {
    width: 50,
    backgroundColor: 'rgba(39,82,82,0.2)',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles;
