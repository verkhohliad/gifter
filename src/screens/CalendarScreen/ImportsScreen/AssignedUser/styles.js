import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  assignedUser: {
    margin: 20,
    flexDirection: 'row',
    alignItems: 'center',
  },
  name: {
    fontSize: 22,
    margin: 10,
  },
  cross: {
    width: 30,
    height: 30,
    alignItems: 'center',
  },
});

export default styles;
