import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  importsScreen: {
    flex: 1,
  },
  title: {
    fontSize: 22,
    textAlign: 'center',
    margin: 20,
  },
});

export default styles;
