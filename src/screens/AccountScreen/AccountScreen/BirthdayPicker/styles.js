import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  birthdayPick: {
    flexDirection: 'row',
    alignItems: 'center',
    margin: 20,
  },
  birthdayLabel: {
    fontSize: 22,
    marginRight: 10,
  },
  iconContainer: {
    borderWidth: 1,
    borderRadius: 20,
  },
});

export default styles;
