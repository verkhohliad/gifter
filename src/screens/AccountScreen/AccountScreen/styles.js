import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  accountScreen: {
    flex: 1,
  },
  userInfo: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  image: {
    margin: 20,
  },
  name: {
    fontSize: 24,
  },
});

export default styles;
