import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wishList: {
    height: 200,
    marginLeft: 20,
  },
  title: {
    fontSize: 22,
  },
  wishItems: {
    flex: 1,
  },
});

export default styles;
